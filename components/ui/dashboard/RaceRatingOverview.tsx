
export default async function RaceRatingOverview() {
    await new Promise(resolve => setTimeout(resolve, 2000))

    // todo 

    // Fetch data from the race_ratings table and calculate the averages for each race
    // Same here maybe add year of the race so we can use it over multiple seasons and compare seasons aswell in the future

    // Slider where first slide shows top 5 rated races this season and the next slide shows the worst 5 rated.
    // A button that opens a modal that shows all the races rated in order



    return (
        <div>RaceRatingOverview</div>
    )
}
